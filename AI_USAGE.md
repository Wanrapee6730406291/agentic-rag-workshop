# AI Usage 
เอกสารบันทึกการประยุกต์ใช้ AI ช่วยในการทำแบบฝึกหัด ข้อผิดพลาดของ AI และบทสรุปความรู้ (Self-Reflection)

## 1. จุดที่ใช้ AI ในการพัฒนา (AI Assistance)

* **การสร้าง Custom Tool:** ให้ AI ช่วยวางโครงสร้างฟังก์ชัน concert_budget_planner และเขียน Docstring ตามมาตรฐาน Google ADK เพื่อให้ LLM ทำ Function Calling ได้ถูกต้อง
* **การตั้งค่า Qdrant Vector Client:** ให้ AI ช่วยแนะนำวิธีการสร้างCollection models.VectorParams และการอัปโหลด PointStruct เข้า In-Memory Qdrant
* **การออกแบบ LLM-as-Judge Prompt:** ให้ AI ช่วยร่าง Prompt สำหรับประเมินคำตอบและบังคับให้ส่งกลับมาเป็น JSON Format ({"score": X, "reason": "..."})

---

## 2. Prompts สำคัญที่ใช้ในการทำงาน

Prompt 1 (สร้าง Tool):
"เขียนฟังก์ชัน Python สำหรับคำนวณงบประมาณคอนเสิร์ต 2,500 บาท รองรับค่าบัตร ค่าเดินทาง ค่ากิน ค่าที่พัก และค่าช้อปปิ้ง พร้อมสร้าง Docstring ละเอียดสำหรับใช้กับ Google ADK FunctionTool"

Prompt 2 (LLM-as-Judge):
"สร้าง JSON Prompt สำหรับให้ Gemini ประเมินความถูกต้องของคำตอบจาก RAG โดยให้คะแนน 1-5 พร้อมระบุเหตุผล"

## 3. กรณีที่ AI แนะนำผิดพลาดและการแก้ไข
1. AI แนะนำชื่อโมเดล:
    สิ่งที่ AI แนะนำ: ในตอนแรก AI แนะนำให้ใช้โมเดล gemini-1.5-flash หรือ gemini-pro ใน Google ADK
    ข้อผิดพลาด: เกิด Error 404 / Model Not Found หรือติด Rate Limit
    การแก้ไข: แก้ไขโดยเปลี่ยนมาใช้โมเดลล่าสุดตามระบบเรียนคือ gemini-3.1-flash-lite ทำให้ระบบทำงานได้อย่างราบรื่นและไม่ติดโควต้า

2. AI ใส่ Prefix Embedding ผิด:
    สิ่งที่ AI แนะนำ: AI ลืมใส่ Prefix query:  และ passage:  ในการแปลงข้อความด้วยโมเดล intfloat/multilingual-e5-large
    ข้อผิดพลาด: ค่า Cosine Similarity Score ต่ำกว่าความเป็นจริง (ได้คะแนนเพียง 0.5-0.6)
    การแก้ไข: ใส่ passage:  นำหน้า Text Chunks และ query:  นำหน้าคำถามตามสเปกของ E5 Model ส่งผลให้ Similarity Score พุ่งขึ้นสูงถึง 0.8842

## 4.reflection 250 คำ
การทำปฏิบัติการ Agentic RAG ในครั้งนี้ ช่วยให้ผมเข้าใจอย่างลึกซึ้งว่า ระบบปัญญาประดิษฐ์ในปัจจุบันไม่ได้เป็นเพียงแค่โมเดลภาษา (LLM) ที่ทำหน้าที่สุ่มคาดเดาคำตอบถัดไปเท่านั้น แต่เป็นการสร้าง "ระบบนิเวศน์ทางปัญญา" (Agentic Ecosystem) ที่ผสานการทำงานระหว่างฐานข้อมูลเชิงความหมาย (Vector Database) เครื่องมือภายนอก (Custom Tools) และการตัดสินใจเชิงตรรกะของ Agent

ในส่วนของ RAG (Retrieval-Augmented Generation) สิ่งสำคัญที่สุดไม่ใช่ตัวโมเดล LLM แต่เป็น "คุณภาพของ Context" การเลือกใช้โมเดล Embedding ที่เหมาะสมอย่าง multilingual-e5-large พร้อมการใส่ Prefix ที่ถูกต้องส่งผลต่อคะแนน Similarity อย่างมีนัยสำคัญ นอกจากนี้ การกำหนดขนาด Chunking และ overlap ก็เป็นจุดสมดุลที่สำคัญ หากตัดขนาดเล็กเกินไป บริบทจะขาดหาย แต่หากตัดขนาดใหญ่เกินไป ก็จะดึง Context Noise เข้ามา ทำให้ LLM เกิดการสับสนหรือตอบไม่ตรงประเด็น

ในส่วนของ Agent & Custom Tools ผมได้เรียนรู้พลังของ Docstring ซึ่งทำหน้าที่เป็นสะพานเชื่อมระหว่างภาษามนุษย์และการรันโค้ดคอมพิวเตอร์ การที่ Agent สามารถอ่าน Docstring แล้ววิเคราะห์ว่าเมื่อไหร่ควรเรียกใช้ฟังก์ชัน concert_budget_planner แสดงให้เห็นถึงก้าวสำคัญของการพัฒนาปัญญาประดิษฐ์ที่ทำงานแทนมนุษย์ได้จริง (Autonomous Agent)

สุดท้าย การประเมินผลด้วย LLM-as-Judge และการวิเคราะห์ Failure Modes ทั้ง 5 รูปแบบ ทำให้ผมตระหนักว่าการสร้างระบบ RAG ให้ใช้งานได้จริงในระดับ Production จำเป็นต้องมีการมอนิเตอร์และปรับแต่งอย่างต่อเนื่อง การแก้ปัญหาด้วยเทคนิค Hybrid Search หรือการกรอง Quality Gate ช่วยยกระดับระบบให้มีความเที่ยงตรงและน่าเชื่อถือมากขึ้นอย่างเห็นได้ชัด ความรู้เหล่านี้จะเป็นรากฐานสำคัญในการนำไปประยุกต์ใช้สร้างระบบ RAG สำหรับองค์กรต่อไปค่ะ
